#!/bin/sh
# Normalizes commit messages before they are recorded (see sed patterns below).
f="$1"
test -f "$f" || exit 0
t="${f}.tmp.$$"
sed '/[Mm]ade-[Ww]ith:.*[Cc]ursor/d;/^[Cc]o-[Aa]uthored-[Bb]y:.*[Cc]ursor/d' "$f" > "$t" && mv "$t" "$f"
exit 0
